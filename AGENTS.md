# TravelPlans 작업 지침

이 폴더의 여행계획을 작성하거나 웹페이지에 여행을 추가할 때 적용한다.

## 계획 원칙

- 유명 장소 나열보다 실제 이동 효율과 여행 만족도를 우선한다.
- 일정은 여유 있게 구성하되 불필요한 공백, 역주행, 같은 권역 재방문을 만들지 않는다.
- 음식점 하나만을 위해 기존 동선에서 벗어났다가 복귀하는 일정을 만들지 않는다.
- 같은 권역의 관광지와 식당은 가능한 한 같은 날 자연스럽게 연결한다.
- 쇼핑은 사용자가 요청한 경우에만 일정에 넣는다.
- 사용자가 확정한 장소, 식당, 출발·도착 시각, 고정 일정은 임의로 변경하지 않는다.
- 같은 권역에 동선이 맞는 식당이 여러 곳이면 한 곳을 임의로 확정하지 않고 당일 선택 가능한 후보로 함께 표시한다.
- 분식·김밥·만두 같은 포장 음식은 식사 대용으로만 보지 않고, 1인분을 나눠 먹는 휴대 간식 후보로도 분류한다.

## 시간과 이동

- 기차·항공편, 체크인·체크아웃, 경기·공연 등 고정 일정을 먼저 배치한다.
- 숙소에서 하루를 시작하고 끝내는 실제 동선과 최종 귀가 동선을 함께 검토한다.
- 대중교통, 택시, 도보 이동시간을 일정표에 별도 행으로 작성한다.
- 현재 지도 정보를 확인해 현실적인 이동시간을 사용하고 임의로 추측하지 않는다.
- 도착 시각과 출발 시각은 범위가 아닌 하나의 명확한 시각으로 맞춘다.
- 지도 예상시간에 여유가 필요하면 이동시간에 5~10분을 반영한다.
- 앞 일정 종료 후 별도 휴식이 없다면 즉시 다음 이동을 시작한다.
- 준비시간은 사용자 지정값을 우선하고 남는 시간을 채우기 위해 임의로 늘리지 않는다.
- 식사는 기본 1시간, 카페와 주변 구경은 기본 1시간 30분으로 잡는다.
- 카페와 구경 장소가 다르면 하나의 행으로 합치지 않는다.
- 관광지 체류시간은 규모와 실제 관람량을 확인한 뒤 정한다.

## 날씨와 대체 일정

- 날씨 영향을 크게 받는 일정은 맑은 날 계획과 비 오는 날 계획으로 분리한다.
- 두 계획의 공통 일정과 대체 일정을 사용자가 구분할 수 있게 표시한다.
- 비 오는 날 계획도 숙소와 고정 일정까지의 이동 효율을 유지한다.

## 일정표 형식

- 일정표 열은 `시간 / 출발지 / 도착지 / 일정 / 비고` 순서로 작성한다.
- 장소가 바뀌는 활동은 행을 분리해 실제 출발지와 도착지가 드러나게 한다.
- `비고`에는 음식 종류·대표 메뉴, 이동수단, 예약·대기, 날씨 주의사항 등 필요한 정보만 쓴다.
- 식당 후보의 `비고`는 `가게명(대표 메뉴 1~2개)` 형식으로 쓰고, 주류나 Codex가 임의로 추천한 메뉴 조합은 넣지 않는다.
- 디저트·분식·포장 음식을 함께 고르는 일정명은 종류를 나열하지 않고 `간식`으로 표시한다.
- 여행 탭 이름은 `YY.MM.DD~YY.MM.DD/지역` 형식을 사용한다.
- 모바일 상세 일정 카드는 시간 다음에 장소 또는 `출발지 → 도착지`를 굵게 표시하고, 일정과 비고는 그 아래 보조 정보로 표시한다.
- 이동 카드에서도 `이동`이라는 일정명보다 실제 출발지와 도착지를 더 강조한다.
- 식당·간식 후보가 여러 곳인 카드는 기본적으로 접어 두고 `점심 후보 4곳`처럼 종류와 개수만 표시한다.
- 후보 카드를 펼치면 가게명은 굵게, 대표 메뉴는 그 아래 보조 글자로 가게별 한 줄씩 구분하며 후보 묶음마다 독립적으로 접고 펼칠 수 있게 한다.
- 커플이 서로 다른 교통편으로 합류하거나 귀가하는 겹치는 일정은 별도 행으로 반복하지 않고 하나의 개인 이동 카드에 함께 표시한다.
- 함께 보는 계획표에서는 관점에 따라 달라지는 `나`, `여자친구` 대신 `수원 출발`, `동탄 출발`, `광명행`처럼 출발지·목적지 기준 배지를 사용하고 합류 시각이나 환승 정보를 카드 안에 표시한다.
- 모바일에서는 날짜 탭 아래 `일정 / 지도` 전환을 제공하고, 기본 화면은 일정으로 표시해 큰 지도와 일정표를 세로로 이어 붙이지 않는다.
- 일정표에서 관광·식사·카페·준비 등 체류 일정은 카드로 유지하고, `이동` 일정은 카드 사이의 얇은 연결선에 이동수단·소요시간·출발지·도착지를 표시한다.
- 일정 화면의 `현재 코스부터`에서 장소를 고르면 해당 장소부터 그날 마지막 장소까지 지도와 일정이 함께 제한되며, `전체 일정`은 두 화면을 모두 전체 범위로 복원한다.
- 하루의 마지막 이동선 뒤에는 최종 도착 장소를 별도 체류 카드로 표시해 일정이 이동 행으로 끝나지 않게 한다. 숙소 복귀일 때는 `숙소(복귀)` 카드에 체크인·휴식 등 도착 후 상태를 표시한다.

## 웹페이지 구조

- 새 여행을 별도 HTML이나 별도 폴더로 분리하지 않고 `index.html` 하단 스크립트의 `trips` 배열에 추가한다.
- 여행 탭 아래에 해당 여행의 날짜별 탭을 표시한다.
- 전체 제목은 `똥뚜 여행계획`을 유지하고, 여행 탭과 중복되는 시작일·종료일은 상단에 다시 표시하지 않는다.
- 상단에는 `이번 여행의 발자국 · 총 약 00km`, 날짜별 동선 제목에는 `약 00.0km` 형식으로 현지 이동거리를 표시한다.
- 현지 이동거리는 첫날 현지 도착 지점부터 마지막 날 귀가 출발 지점까지 지도에 등록된 날짜별 전체 동선을 기준으로 하며, 출발지에서 여행지까지와 귀가 열차·항공 거리는 제외한다.
- 이동거리는 실제 도로 경로 응답을 합산하고 케이블카 또는 경로 조회 실패 구간만 좌표 간 거리로 보완하며, 일부 동선만 선택해도 날짜별 전체 거리 표시는 바꾸지 않는다.
- 식당 등 택일 동선이 있으면 모든 후보 경로를 중복 합산하지 않고 각 경로를 별도로 계산한 뒤 대표 중간값 하나를 `약`과 함께 표시한다. 상단 총거리는 정수 km로 반올림한다.
- WindowTools `DefaultThemeCatalog`의 기본 다크 팔레트와 `Segoe UI`를 사용한다.
- 페이지는 CSS와 JavaScript를 `index.html`에 포함한 단일 파일로 유지한다.
- 화면은 아이폰 12의 CSS 해상도인 390×844를 기준으로 작성하고 일정표를 가로 스크롤 방식으로 만들지 않는다.
- 지도는 Leaflet과 OpenStreetMap 실제 지도를 사용한다.
- 차량과 도보는 실제 도로 경로를 표시하고 케이블카 등 특수 이동만 별도 파선으로 표시한다.
- 방문 장소에는 일정 순서 번호를 표시한다.
- 지도에서 시작·도착 번호를 선택하면 해당 구간의 마커와 동선뿐 아니라 아래 상세 일정도 같은 범위로 제한한다.
- `전체 보기`는 지도와 상세 일정을 모두 해당 날짜의 전체 범위로 복원한다.
- 같은 장소를 다시 방문하면 장소 고유번호를 재사용하지 않고 방문 순서에 따라 새 번호를 부여한다.
- 출발과 복귀처럼 같은 좌표의 방문지가 함께 표시될 때는 `1·9`처럼 하나의 마커에 번호를 합치고, 선택창과 일정에서는 `숙소(출발)`, `숙소(복귀)`처럼 역할을 구분한다.
- 지도 경로 서버 실패 시 해당 구간만 직선으로 대체하고 상태를 사용자에게 알린다.

## 변경 범위와 Git

- 여행 추가·수정 요청에서는 이 저장소 밖의 파일을 변경하지 않는다.
- GitHub Pages는 `main` 브랜치의 저장소 루트에서 배포하며 공개 주소 `https://aassder95.github.io/TravelPlans/`을 유지한다.
- 별도 배포 워크플로를 만들지 않고 GitHub Pages의 `Deploy from a branch` 설정을 사용한다.
- `robots.txt`, `.nojekyll`, `index.html`의 검색엔진 수집 방지 설정을 유지한다.
- 공개 페이지에는 검색엔진 수집 방지 설정을 유지하고, 인증 기반 비공개 페이지가 아니라 링크를 아는 사람이 접속하는 방식임을 안내한다.
- 작업 중 사용자가 확정한 재사용 가능한 계획 기준이나 웹 동작은 같은 작업에서 이 문서에 반영하고, 사용 방법 설명이 필요하면 `README.md`도 함께 갱신한다.
- 이 저장소의 여행계획 추가·수정 요청은 별도 중단 지시가 없으면 변경 파일을 검증한 뒤 `main`에 commit·push하고, GitHub Pages 공개 주소에서 새 내용이 보이는 것까지 확인한다.
